<!DOCTYPE html>

<html>
    <head>
    <% include ../partials/head %>
    </head>
    <body>
        <% include C:\Users\rim\Desktop\nodejs\contact-book\views\partials\nav.ejs %>
        <div class="contact contact-edit">
            <div class="contact-menu">
                <a href="/contacts/<%=contact._id %>">Back</a>
            </div>
            <h2>Edit</h2>
            <form class="contact-form" action="/contacts/<%=contact._id %>?_method=put" method="post">
                <div>
                    <label for="name">Name: </label>
                    <input type="text" id="name" name="name"  value="<%= contact.name %>">
                </div>
                <div>
                    <label for="email">Email</label>
                    <input type="text" id="email" name="email" value="<%= contact.email %>">
                </div>
                <div>
                    <label for="phone">Phone</label>
                    <input type="test" name="phone" id="phone" value="<%= contact.phone%>">
                </div>
                <div>
                    <button type="submit">submit</button>
                </div>
       </div>
            </form>
    </body>
</html>