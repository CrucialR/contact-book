<!DOCTYPE html>
<html>
    <head>
    <% include ../partials/head %>
    </head>
    <body>
         <% include C:\Users\rim\Desktop\nodejs\contact-book\views\partials\nav.ejs %>

        <div class="contact contact-index">
        <h2>index</h2>
        <ul>
            <% contacts.forEach(function(contact){ %>
                <li>
                  <a href="/contacts/<%= contact._id %>"><%= contact.name %></a> <!-- 1 -->
                </li>
            <% }) %>
        </ul>
        </div>
    </body>
</html>